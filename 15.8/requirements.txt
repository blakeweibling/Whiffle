# Runtime dependencies for Whiffle.
# Python 3.10+ supported; Python 3.13 is what's running on the Pi.
# Install with: pip install -r requirements.txt
#
# Floors below are chosen so pip is guaranteed to pick a build that ships
# prebuilt cp313 wheels (and cp313 + aarch64 wheels where ARM matters).

# Core CV / game stack
numpy>=1.24.0           # build_pi.sh forces <2 on Py 3.10/3.11; >=2 on Py 3.12+
opencv-python>=4.10.0   # first release with cp313 wheels
pygame>=2.6.0           # first release with cp313 wheels
ultralytics>=8.3.0      # first line with Py 3.13 support; pulls torch>=2.5 transitively

# Scientific (optional at runtime -- tracking.py guards with try/except)
scipy>=1.14.0           # first release with cp313 wheels (incl. aarch64)

# HTTP / config
requests>=2.31.0
urllib3>=1.26.0
httplib2>=0.20.4
python-dotenv>=1.0.0
setuptools>=68.0.0

# Google APIs (Drive + YouTube uploads)
google-api-python-client>=2.80.0
google-auth>=2.16.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.0.0
oauth2client>=4.1.3     # deprecated by Google in 2017 but still functional on 3.13;
                        # only used by youtube_utils.py's legacy upload flow.
