# -*- mode: python ; coding: utf-8 -*-

import sys
import os

added_files = [
    # Root directory files
    ('.env', '.'),
    ('achievements.json', '.'),
    ('achievements_status.json', '.'),
    ('data.yaml', '.'),
    ('game_over.png', '.'),
    ('high_scores.json', '.'),
    ('hsv_ranges.json', '.'),
    ('logo.png', '.'),
    ('pinball_icon.png', '.'),
    ('scoring_zones.json', '.'),
    ('splash.jpg', '.'),
    ('splash.png', '.'),
    ('static_frame.png', '.'),
    ('whiffle_leaderboard.json', '.'),
    ('whiffle_new_best.pt', '.'),

    # Folders and their contents
    ('sounds', 'sounds'),
]

a = Analysis(
    ['game.py'], # Your main script filename
    pathex=[],
    binaries=[],
    datas=added_files,
    hiddenimports=[
        'pygame',
        'numpy',
        'cv2',
        'ultralytics',
        'scipy.spatial',
        'requests',
        'setuptools',
        'queue',
        'json',
        'logging',
        'datetime',
        'time',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[ # Verify these exclusions during testing
        'tkinter', 'sqlite3', 'PyQt5', 'unittest', 'pytest',
        'pydoc_data', 'notebook', #'dotenv', #'scipy', #'requests',
        #'urllib3', 'chardet', 'idna', 'certifi', '_ssl',
    ],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=None,
    noarchive=False,
)

pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    [],
    a.datas,
    [],
    name='WhiffleTracker',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=False,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False, # Set True for console window, False for GUI only
    disable_windowed_traceback=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='pinball_icon.ico'
)

# --- For 'onedir' mode ---
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=False,
    upx_exclude=[],
    name='WhiffleTracker_App', # Name of the output folder
)